# Life OS · 第二阶段修复报告（动画重放 + 死卡）

> 执行时间：2026/08/02
> 依据标准：`ARCHITECTURE.md`（v1.0.0，唯一产品标准）
> 范围边界：**只修交互问题，不做功能开发**
> - 改：页面跳转 / 卡片入口点击 / 必要的动画触发逻辑
> - 不改：UI 设计 / 主题系统 / 代码重构 / 不新增功能
> 改动规模：**10 个 HTML 文件，+280 / −24（净 +256）**，CSS 零手写新增（动画逻辑由注入脚本在运行时控制）

---

## 一、两个问题与根因

| # | 现象 | 根因 |
|---|------|------|
| ① | 页面切换重复播放入场动画，像重新打开 App | 多页面架构下每次 `location.href` 都是整页重载，`@keyframes rise` 在每个页面加载时无条件重放 |
| ② | 页面内部卡片无法点击 | 首页「核心管理」网格里 **「习惯追踪」「数据统计」是已删除功能（架构 1.4）的残留卡**，长着入口卡的样子（带 `›` 箭头）却**没有 `onclick`、也没有目标页**，点了毫无反应 |

---

## 二、修改文件清单

10 个 HTML 文件均做了同一处注入（门控脚本），仅 `index.html` 额外删除了 2 张死卡。

| 文件 | 改动 | 说明 |
|------|------|------|
| `index.html` | +28 / −24 | 注入门控脚本；**删除「习惯追踪」「数据统计」2 张死卡**（核心管理网格现剩 6 张有效入口卡） |
| `space.html` | +28 | 注入门控脚本 |
| `app-framework.html` | +28 | 注入门控脚本 |
| `companion.html` | +28 | 注入门控脚本 |
| `cycle.html` | +28 | 注入门控脚本 |
| `finance.html` | +28 | 注入门控脚本 |
| `decide.html` | +28 | 注入门控脚本 |
| `inspiration.html` | +28 | 注入门控脚本 |
| `profile.html` | +28 | 注入门控脚本 |
| `task-center.html` | +28 | 注入门控脚本 |

---

## 三、每个文件具体改了什么

### 3.1 入场动画门控脚本（10 个文件统一注入，位置：`<head>` 内 `theme-inline.js` 之后）

```html
<script>
(function(){try{
  function lifeosSuppress(){
    // 只把「当前正在播放入场动画」的元素逐个设为 animation:none（内联），
    // 不影响用户后续触发的动画（如 task-center 完成脉冲、inspiration 新建卡片滑入）
    var de=document.documentElement;
    de.classList.remove('lifeos-noentrance');
    var all=document.querySelectorAll('*'),t=[];
    for(var i=0;i<all.length;i++){var n=getComputedStyle(all[i]).animationName;if(n&&n!=='none')t.push(all[i]);}
    de.classList.add('lifeos-noentrance');
    for(var k=0;k<t.length;k++){t[k].style.animation='none';}
    var st=document.getElementById('lifeos-suppress');if(st)st.parentNode.removeChild(st);
    de.classList.remove('lifeos-noentrance');
  }
  if(sessionStorage.getItem('lifeos_inapp')){
    // 应用内导航抵达：先广域抑制防闪，DOM 就绪后精确抑制并撤掉临时规则
    var s=document.createElement('style');s.id='lifeos-suppress';
    s.textContent='html.lifeos-noentrance *{animation:none!important}';
    document.head.appendChild(s);
    document.documentElement.classList.add('lifeos-noentrance');
    sessionStorage.removeItem('lifeos_inapp');
    if(document.readyState!=='loading')lifeosSuppress();
    else document.addEventListener('DOMContentLoaded',lifeosSuppress);
  }
  // 任意点击若落在带 location.href 的入口上，标记「这是应用内导航」
  document.addEventListener('click',function(e){
    var t=e.target;
    while(t&&t!==document){var o=(t.getAttribute&&t.getAttribute('onclick'))||'';if(/location\.href/.test(o)){try{sessionStorage.setItem('lifeos_inapp','1');}catch(_){}break;}t=t.parentElement;}
  },true);
}catch(_){}})();
</script>
```

**效果：**
- **冷启动 / 刷新**（无标记）→ 入场动画照常播放。
- **Tab / 入口卡点击跳转**（标记置位）→ 下一页加载时入场动画被抑制，不再「像重新打开 App」；且采用「精确抑制」而非粗暴 `*` 覆盖，所以**不会迟发、也不会误杀用户交互动画**。
- 临时 class 与临时 `<style>` 在 DOM 就绪后立刻清理，无残留。

### 3.2 删除 2 张死卡（`index.html`）

移除「核心管理」网格中「习惯追踪」「数据统计」两块——它们对应的「习惯追踪模块 / 成长系统」已在架构文档 1.4「已移除功能」中明确删除，且无任何目标页，属于上一轮体检标记的「文案/功能残留」。删除后网格剩 6 张有效入口卡（我的伙伴 / 财富规划 / 购物清单 / 随心记录 / 身体状态 / 资料库），全部带有效 `onclick`。

---

## 四、真机模拟验证（Playwright · 393×852 · DPR3 · 触摸 · standalone）

| 项目 | 结果 |
|------|------|
| 冷启动入场动画 | space `.page-head`/`.section` = `rise` ✅ 正常播放 |
| 应用内跳转抑制 | companion `.pet-card` @300ms=`none`、@1400ms=`none` ✅ 被抑制且无迟发 |
| 临时痕迹清理 | `html` class 清空、临时 `<style>` 已移除 ✅ |
| 直接冷打开子页 | companion `.pet-card` = `rise` ✅ 仍播放 |
| 首页入口卡跳转 | 6/6 ✅ |
| 空间入口卡跳转 | 7/7 ✅ |
| 死卡消失 | 首页不再含「习惯追踪 / 数据统计」 ✅ |
| JS 报错 | 无 ✅ |

---

## 五、边界遵守

- ✅ 未改任何手写 CSS 视觉规则（动画触发改由注入脚本运行时控制）
- ✅ 未动主题系统（`theme-inline.js` / `theme-runtime.js` 一字未改）
- ✅ 未新增功能、未恢复已删除系统
- ✅ 未重构架构，仅在 `<head>` 注入一小段原生脚本

---

## 六、状态说明

- 改动**未提交、未推送**（沿用上一轮约定：等你确认后再处理提交/推送）。
- 本轮仅解决你提出的两个交互 bug。其余上轮遗留项（真实 `record` 页、`+` 共用弹窗、companion 用药记录、task-center 等级/EXP 残留等）仍按原边界**未处理**。
- 关于「页面内部卡片无法点击」：已确认的死卡只有首页这 2 张（已删）。各子页的内容卡（状态卡 / 计划卡 / 分配卡 / 灵感卡 / 任务卡等）按架构是**纯展示卡、非入口**，本身不导航，属正常设计；如你希望其中某些也变成可点击入口，请告知目标页，我再按需接线。
